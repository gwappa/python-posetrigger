# !CAUTION!
# This environment can not perform pose estimation!
# Use it only for testing purposes.
#
# run: conda env create -f nodlc-posetrigger.yaml
name: posetrigger-nodlc
dependencies:
    - python=3.7
    - numpy
    - matplotlib
    - opencv
    - PyQt
    - cython
    - pip
    - pip:
        - timedcapture
        - pose-trigger
